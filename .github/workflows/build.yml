name: Build

on:
  push:
    branches: [master]
  pull_request:

jobs:
  build-flipper:
    name: Build firmware (flipper/, esp32c6)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build firmware for esp32c6
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v5.3.1
          target: esp32c6
          path: 'MakeshiftFlipper'

  host-tests:
    name: Host logic tests (MakeshiftFlipper/tests/)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run host tests
        run: bash MakeshiftFlipper/tests/run_tests.sh
