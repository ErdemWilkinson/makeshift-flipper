#include "wifi_scan.h"

// No functions defined yet -- see wifi_scan.h. Not added to
// c6-firmware/main/CMakeLists.txt's SRCS until it has real content (an
// empty translation unit compiles fine, but there's nothing to link
// against yet).
